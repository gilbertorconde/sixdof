[package]
name = "sixdof"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.98"
description = "Dependency-free client for 6-degree-of-freedom input devices (SpaceMouse, SpaceNavigator, Spaceball) on Unix."
keywords = ["spacenavd", "6dof", "input", "cad"]
categories = ["hardware-support"]
readme = "README.md"

[features]
default = []
# The second backend: the Magellan protocol, spoken over a display-server
# connection by the vendor's own driver as well as by spacenavd.
magellan = ["dep:x11rb"]

[dependencies]
x11rb = { version = "0.13", optional = true }
